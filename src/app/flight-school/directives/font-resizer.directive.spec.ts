import { FontResizerDirective } from './font-resizer.directive';

describe('FontResizerDirective', () => {
  it('should create an instance', () => {
    const directive = new FontResizerDirective();
    expect(directive).toBeTruthy();
  });
});
